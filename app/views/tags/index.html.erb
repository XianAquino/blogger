<h1>All tags</h1>

<ul id = "tags">
	<% @tags.each do |tag| %>
		<li><%= tag.name %>
			<%= link_to "Delete" , tag_path(tag), method: :delete,
				data: {confirm: "Are you sure you want to delete this tag"} %>
		</li>
	<% end %>
</ul>
